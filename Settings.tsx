// View tracking is done manually by admin
export function startViewTracking() {
  // Manual view tracking - admin updates views directly in Supabase
  console.log('View tracking: manual mode')
}

export async function trackSubmissionViews() {
  // Admin manually updates views via Supabase dashboard
}
